<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>cn.daxpay.open</groupId>
        <artifactId>daxpay-platform-service</artifactId>
        <version>4.0.0-beta4</version>
    </parent>

    <artifactId>service-iam</artifactId>
    <description>身份识别与访问管理</description>

    <dependencies>

        <!-- 认证 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-auth</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 第三方社交登录 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-social</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 支付宝开放平台能力(AlipaySocialAuthRequest 授权登录使用) -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-alipay</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 微信公众号能力(WechatMpAuthEndpoint 授权登录使用 WechatMpAuthService) -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-wechat</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 抖音开放平台能力(小程序快捷登录 SocialAppletAuthService 使用 DouyinMaAuthService) -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-douyin</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 防重放Nonce验证 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>capability-nonce</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 请求上下文 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>common-request-context</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 系统基础服务 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>service-baseapi</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 平台配置服务 -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>service-system</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 邮件通知(邮箱绑定验证/找回密码验证码发送) -->
        <dependency>
            <groupId>cn.daxpay.open</groupId>
            <artifactId>service-notify</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 单元测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- hutool验证码 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-captcha</artifactId>
            <version>${hutool.version}</version>
        </dependency>
        <!-- WebAuthn 通行密钥服务端核心(注册/断言验证, 纯 Java 无 Spring Security 绑定) -->
        <dependency>
            <groupId>com.yubico</groupId>
            <artifactId>webauthn-server-core</artifactId>
            <version>${webauthn-server.version}</version>
        </dependency>
    </dependencies>

</project>
